# demo
```python
git show
git reset
check out
rm --cached
(index|stage)
git push

#冲突
git clone ~ [othername]
git pull
fix the commit

git branch <branchname>
git checkout <branchname> 切块
合并git checkout -b <branchname>

git pull
git merge branch
git push

git merge origin/branch2
#ide完成合并

#fork？
```

